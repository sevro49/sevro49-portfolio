import Hero from "./Hero";
import SelectedWorks from "./SelectedWorks";

const Home = () => {
  return (
    <main className="w-full">
      <Hero />
      <SelectedWorks />
    </main>
  );
};

export default Home;
