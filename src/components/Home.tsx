import Hero from "./Hero";
import SelectedWorks from "./SelectedWorks";

const Home = () => {
  return (
    <main id="home" className="w-full">
      <Hero />
      <SelectedWorks />
    </main>
  );
};

export default Home;
